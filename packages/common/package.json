{
  "name": "common",
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "^27.2.2",
    "jest-mock-extended": "^2.0.4",
    "ts-jest": "^27.0.5",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "compile": "tsc --noEmit",
    "test": "jest --runInBand",
    "test:watch": "jest --runInBand --watchAll"
  },
  "dependencies": {
    "@anime-skip/api-client": "^2.6.2"
  }
}
