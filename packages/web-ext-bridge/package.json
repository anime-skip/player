{
  "name": "@anime-skip/web-ext-bridge",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "compile": "tsc --noEmit",
    "lint": "pnpm lint:check -- --fix",
    "lint:check": "pnpm eslint . --ext .ts,.vue --max-warnings 0"
  },
  "dependencies": {
    "webextension-polyfill": "*"
  },
  "devDependencies": {
    "@anime-skip/eslint-config": "workspace:*",
    "typescript": "*"
  }
}
