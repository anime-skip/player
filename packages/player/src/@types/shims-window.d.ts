interface Window {
  /**
   * Styles are loaded onto the `window` object so they can be applied at runtime without needing to
   * bundle the CSS file separately.
   */
  animeSkipPlayerCss?: string;
}
