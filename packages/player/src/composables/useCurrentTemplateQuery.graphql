query findTemplateByDetails(
  $episodeId: ID
  $showName: String
  $season: String
) {
  findTemplateByDetails(
    episodeId: $episodeId
    showName: $showName
    season: $season
  ) {
    ...Template
  }
}
