{
  "name": "@anime-skip/inspector",
  "private": true,
  "scripts": {
    "start": "TARGET=firefox vite build --watch",
    "build": "TARGET=firefox vite build",
    "start:chrome": "TARGET=chrome vite build --watch",
    "build:chrome": "TARGET=chrome vite build"
  },
  "devDependencies": {
    "@types/lodash.debounce": "^4.0.7",
    "typescript": "^4.6.4",
    "vite": "^2.9.8",
    "vite-plugin-web-extension": "^1.3.2"
  },
  "dependencies": {
    "lodash.debounce": "^4.0.8",
    "webextension-polyfill": "^0.8.0"
  }
}
