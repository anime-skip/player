{
  "name": "anime-skip-player-monorepo",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "preinstall": "npx only-allow pnpm",
    "validate": "pnpm format:check",
    "prepare": "husky install"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@types/download",
        "body-parser",
        "download",
        "express",
        "safe-compare"
      ]
    }
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "prettier": "^2.6.2",
    "pretty-quick": "^3.1.3"
  }
}
