# Firefox Source Code Review

Required node version listed in `.tool-versions` file, required PNPM version
listed in `package.json`'s `packageManager` field.

To build the extension and zip it:

```sh
pnpm i
cd apps/extension
pnpm zip:firefox
```
